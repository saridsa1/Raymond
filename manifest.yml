---
declared-services:
  raymond:
    label: raymond
    plan: free
applications:
- name: raymond
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - raymond
  env:
    NPM_CONFIG_PRODUCTION: false
